# Release Strategy

- Versioning: Semantic Versioning (`MAJOR.MINOR.PATCH`).
- Source of truth: `pyproject.toml` `project.version`.
- Release trigger: Git tag `vX.Y.Z` on main branch.
- Changelog policy: user-visible behavior changes are documented in release notes.
- Hotfix policy: `PATCH` only, no breaking changes.
